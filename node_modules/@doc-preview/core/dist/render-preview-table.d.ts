/** Shared helpers for tabular preview renderers (CSV, XLSX). */
export declare const PREVIEW_TABLE_MAX_ROWS = 2500;
export type DataTableEnhanceOptions = {
    /** Freeze the first row and keep it visible while scrolling. */
    stickyHeader?: boolean;
    /** Drag handles on header cells to resize columns. */
    columnResize?: boolean;
    minColumnWidthPx?: number;
};
export declare function appendTableNotice(host: HTMLElement, message: string): void;
export declare function enhanceDataTable(table: HTMLTableElement, options?: DataTableEnhanceOptions): void;
//# sourceMappingURL=render-preview-table.d.ts.map