export declare class WorkerBridge<TRequest extends object, TResponse> {
    private readonly createWorker;
    private worker;
    private pending;
    private msgCounter;
    constructor(createWorker: () => Worker);
    private ensureWorker;
    call(request: TRequest, transfer?: Transferable[]): Promise<TResponse>;
    terminate(): void;
}
/** Returns true if Worker API is available (not in SSR/restricted environments). */
export declare function workerApiAvailable(): boolean;
//# sourceMappingURL=worker-bridge.d.ts.map