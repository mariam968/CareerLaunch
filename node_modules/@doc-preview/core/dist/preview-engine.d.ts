import type { LoadedPreview, PreviewConfig, PreviewDocument, PreviewHostOptions, PreviewPhase, PreviewPhaseEvent, PreviewRenderer, ResolvedPreviewDocument } from "./types";
export type PreviewEngineState = {
    phase: PreviewPhase;
    activeIndex: number;
    documents: PreviewDocument[];
    resolved: ResolvedPreviewDocument | null;
    renderer: PreviewRenderer | null;
    loaded: LoadedPreview | null;
    errorText: string;
    /** -1 = indeterminate, 0..1 = real byte progress */
    loadProgress: number;
};
export declare class PreviewEngine {
    private readonly options;
    private readonly onRefresh?;
    private state;
    private abort;
    private cleanupRender;
    private revokeUrls;
    private progressCallback;
    constructor(options: PreviewHostOptions, onRefresh?: (() => void) | undefined);
    getState(): Readonly<PreviewEngineState>;
    getConfig(): PreviewConfig | undefined;
    setProgressCallback(cb: ((p: number) => void) | null): void;
    setDocuments(documents: PreviewDocument[]): void;
    setActiveIndex(index: number): void;
    next(): void;
    prev(): void;
    retry(): void;
    emitPhase(): PreviewPhaseEvent;
    private setPhase;
    private clearLoaded;
    loadActive(): Promise<void>;
}
//# sourceMappingURL=preview-engine.d.ts.map