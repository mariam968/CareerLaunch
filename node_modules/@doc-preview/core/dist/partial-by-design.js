import { detectDocumentType } from "./type-sniff.js";
const COMPARE_TYPES = new Set([
    "txt",
    "text",
    "plain",
    "csv",
    "html",
    "htm",
    "json",
    "xml",
    "md",
    "js",
    "ts",
    "css",
    "docx",
    "docm",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document.macroEnabled.12",
    "application/vnd.ms-word.document.macroEnabled.12",
    "doc",
    "application/msword",
    "pdf",
    "application/pdf",
    "xlsx",
    "xls",
    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    "application/vnd.ms-excel",
    "pptx",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    "ppt",
    "application/vnd.ms-powerpoint",
    "odt",
    "application/vnd.oasis.opendocument.text",
    "ods",
    "application/vnd.oasis.opendocument.spreadsheet",
    "odp",
    "application/vnd.oasis.opendocument.presentation"
]);
const SEARCH_TYPES = new Set([
    "pdf",
    "application/pdf",
    "docx",
    "docm",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document.macroEnabled.12",
    "application/vnd.ms-word.document.macroEnabled.12",
    "pptx",
    "pptm",
    "potx",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation.macroEnabled.12",
    "odt",
    "application/vnd.oasis.opendocument.text",
    "odp",
    "application/vnd.oasis.opendocument.presentation",
    "xlsx",
    "xlsm",
    "xltx",
    "xls",
    "ods",
    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    "application/vnd.ms-excel",
    "application/vnd.oasis.opendocument.spreadsheet"
]);
const NATIVE_DIFF_TYPES = new Set([
    "pdf",
    "application/pdf",
    "docx",
    "docm",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document.macroEnabled.12",
    "application/vnd.ms-word.document.macroEnabled.12",
    "pptx",
    "pptm",
    "potx",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation.macroEnabled.12",
    "odt",
    "application/vnd.oasis.opendocument.text",
    "odp",
    "application/vnd.oasis.opendocument.presentation",
    "xlsx",
    "xlsm",
    "xltx",
    "xls",
    "ods",
    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    "application/vnd.ms-excel",
    "application/vnd.oasis.opendocument.spreadsheet"
]);
export const PARTIAL_BANNERS = {
    pptxFidelity: "Slide layout shows positioned text, shapes, and charts — 3D charts use embedded images or pseudo-3D SVG when data is unavailable.",
    pptxAnimations: "Build steps and slide transitions play in simplified form; full OOXML animation timing is not replicated.",
    cloudIframe: "Cloud embeds cannot detect iframe load failures (browser CORS). If the preview looks blank, use Try alternate viewer below or open the document in a new tab.",
    splitDiffText: "Text-extraction diff — extracted plain text with line highlights, not overlays on the native viewer.",
    splitDiffNativeDocx: "Native diff — inline word highlights on DOCX paragraphs. Alignment may differ from extracted text.",
    splitDiffNativePdf: "Native diff — word-level highlights positioned from the PDF text layer.",
    splitDiffNativePptx: "Native diff — slide text and positioned shapes/charts highlighted in the deck preview.",
    splitDiffNativeOdt: "Native diff — word highlights and layout markers on ODT HTML blocks. Complex layout may differ from LibreOffice.",
    splitDiffNativeSpreadsheet: "Native diff — row and cell highlights on the active worksheet table.",
    splitDiffNativeOdp: "Native diff — slide text and positioned elements highlighted in the ODP deck preview.",
    scannedPdf: "This PDF has little or no text layer — compare and search need OCR or a text-based export. Image-only pages cannot be diffed at word level.",
    compareUnsupported: "Compare uses text extraction for documents. Matching image, video, or audio pairs use a metadata / side-by-side preview instead.",
    compareHint: "Compare extracts plain text for Office/PDF/text. Upload matching image, video, or audio types for metadata comparison.",
    legacyOffice: "Legacy Office formats use text or HTML extraction — not layout-faithful rendering. For full layout, use a public URL with the cloud viewer or configure server-side conversion (LibreOffice, Gotenberg, or your API via config.conversion).",
    openDocument: "OpenDocument (.odt, .ods, .odp) preview uses in-browser HTML extraction — complex layouts, macros, and embedded objects may differ from LibreOffice. For exact output, export to PDF or DOCX.",
    spreadsheetOds: "ODS preview uses the same multi-sheet tab UI as XLSX (SheetJS). Complex formulas, charts, and macros may not render."
};
export function isCompareSupportedDocument(doc) {
    const type = detectDocumentType(doc);
    return COMPARE_TYPES.has(type);
}
export function isSearchSupportedDocument(doc) {
    const type = detectDocumentType(doc);
    return SEARCH_TYPES.has(type);
}
export function getSplitDiffMode(left, right) {
    const a = detectDocumentType(left);
    const b = detectDocumentType(right);
    if (a !== b || !NATIVE_DIFF_TYPES.has(a)) {
        return "text";
    }
    if (a === "pdf" || a === "application/pdf") {
        return "native-pdf";
    }
    if (a === "pptx" || a === "pptm" || a === "potx" || a === "application/vnd.openxmlformats-officedocument.presentationml.presentation") {
        return "native-pptx";
    }
    if (a === "odt" || a === "application/vnd.oasis.opendocument.text") {
        return "native-odt";
    }
    if (a === "odp" || a === "application/vnd.oasis.opendocument.presentation") {
        return "native-odp";
    }
    if (a === "xlsx" ||
        a === "xlsm" ||
        a === "xltx" ||
        a === "xls" ||
        a === "ods" ||
        a === "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" ||
        a === "application/vnd.ms-excel" ||
        a === "application/vnd.oasis.opendocument.spreadsheet") {
        return "native-xlsx";
    }
    return "native-docx";
}
export function splitDiffBannerMessage(mode) {
    if (mode === "native-pdf") {
        return PARTIAL_BANNERS.splitDiffNativePdf;
    }
    if (mode === "native-pptx") {
        return PARTIAL_BANNERS.splitDiffNativePptx;
    }
    if (mode === "native-odt") {
        return PARTIAL_BANNERS.splitDiffNativeOdt;
    }
    if (mode === "native-odp") {
        return PARTIAL_BANNERS.splitDiffNativeOdp;
    }
    if (mode === "native-xlsx") {
        return PARTIAL_BANNERS.splitDiffNativeSpreadsheet;
    }
    if (mode === "native-docx") {
        return PARTIAL_BANNERS.splitDiffNativeDocx;
    }
    return PARTIAL_BANNERS.splitDiffText;
}
export function createPartialBanner(message, title = "Partial preview") {
    const el = globalThis.document.createElement("p");
    el.className = "dp-partial-banner";
    el.setAttribute("role", "note");
    const heading = globalThis.document.createElement("strong");
    heading.className = "dp-partial-banner__title";
    heading.textContent = title;
    const body = globalThis.document.createElement("span");
    body.className = "dp-partial-banner__text";
    body.textContent = message;
    el.append(heading, body);
    return el;
}
export function prependPartialBanner(host, message, title) {
    host.prepend(createPartialBanner(message, title));
}
/** Default true — set `config.office.showPartialPreviewBanner` to false to hide Office partial banners. */
export function isPartialPreviewBannerEnabled(config) {
    return config?.office?.showPartialPreviewBanner !== false;
}
/** Default true — set `config.pdf.showAnnotateToolbar` to false to hide the PDF Annotate toolbar. */
export function isAnnotateToolbarEnabled(config) {
    return config?.pdf?.showAnnotateToolbar !== false;
}
/** Default true — set `config.pdf.showReviewBadge` to false to hide the review-state badge. */
export function isReviewBadgeEnabled(config) {
    return config?.pdf?.showReviewBadge !== false;
}
export function appendPartialPreviewBannerIfEnabled(host, config, message, title) {
    if (!isPartialPreviewBannerEnabled(config)) {
        return;
    }
    host.append(createPartialBanner(message, title));
}
export function prependPartialPreviewBannerIfEnabled(host, config, message, title) {
    if (!isPartialPreviewBannerEnabled(config)) {
        return;
    }
    host.prepend(createPartialBanner(message, title));
}
//# sourceMappingURL=partial-by-design.js.map