const renderers = [];
export function registerPreviewRenderer(renderer) {
    const existing = renderers.findIndex((r) => r.id === renderer.id);
    if (existing >= 0) {
        renderers.splice(existing, 1, renderer);
    }
    else {
        renderers.push(renderer);
    }
    renderers.sort((a, b) => b.weight - a.weight);
}
export function listPreviewRenderers() {
    return renderers;
}
export function findPreviewRenderer(doc) {
    const matches = renderers.filter((r) => r.supports(doc));
    if (!matches.length) {
        return renderers.find((r) => r.id === "fallback");
    }
    return matches.sort((a, b) => b.weight - a.weight)[0];
}
export const BUILTIN_PREVIEW_RENDERER_IDS = [
    "pdf",
    "docx",
    "image",
    "media",
    "html",
    "csv",
    "text",
    "fallback"
];
//# sourceMappingURL=registry.js.map