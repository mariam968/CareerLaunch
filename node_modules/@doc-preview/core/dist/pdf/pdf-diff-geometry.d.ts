export type PdfPageTextBounds = {
    page: number;
    /** Normalized 0–1 within page (origin top-left). */
    left: number;
    top: number;
    width: number;
    height: number;
};
export type PdfTextFragment = PdfPageTextBounds & {
    text: string;
};
export type PdfDiffGeometry = {
    /** Same string as `extractPdfPlainText`. */
    text: string;
    /** Page number (1-based) for each line from `text.split(/\r?\n/)`; null for blank separators. */
    linePages: (number | null)[];
    pageBounds: Map<number, PdfPageTextBounds>;
    /** Text fragments with bounds, grouped by page. */
    pageFragments: Map<number, PdfTextFragment[]>;
};
/** Line/page alignment for the same plain text used in document compare. */
export declare function extractPdfDiffGeometry(buffer: ArrayBuffer): Promise<PdfDiffGeometry>;
export declare function pdfDiffPageForSideLine(sideText: string, linePages: (number | null)[], lineValue: string | undefined): number | null;
/** Word-level highlight bounds for a diff line on one PDF side. */
export declare function pdfDiffBoundsForWords(geometry: PdfDiffGeometry, sideText: string, lineValue: string, words: string[]): PdfPageTextBounds[];
//# sourceMappingURL=pdf-diff-geometry.d.ts.map