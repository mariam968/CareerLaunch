import { openDpModal } from "../ui/modal.js";
export function promptPdfPassword(host, reason, onSubmit) {
    const field = globalThis.document.createElement("input");
    field.type = "password";
    field.className = "dp-field";
    field.placeholder = "PDF password";
    field.autocomplete = "current-password";
    const reasonText = reason === 2
        ? "This PDF requires a password to open."
        : "The password you entered was incorrect. Try again.";
    openDpModal(host, {
        title: "Unlock PDF",
        description: reasonText,
        content: field,
        actions: [
            { label: "Cancel", variant: "secondary", onClick: () => undefined },
            {
                label: "Unlock",
                variant: "primary",
                onClick: () => {
                    const value = field.value;
                    if (!value) {
                        field.focus();
                        return false;
                    }
                    onSubmit(value);
                }
            }
        ]
    });
    queueMicrotask(() => field.focus());
}
//# sourceMappingURL=pdf-password-ui.js.map