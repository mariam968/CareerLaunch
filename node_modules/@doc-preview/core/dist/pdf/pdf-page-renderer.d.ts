import type { PDFDocumentProxy, PDFPageProxy } from "pdfjs-dist";
/** Target width for print rasterization (~150 DPI on A4 with 8mm margins). */
export declare const PRINT_PAGE_WIDTH_PX = 1240;
/** Max height so a single PDF page fits one printed sheet when combined with print CSS. */
export declare const PRINT_PAGE_MAX_HEIGHT_PX = 1680;
export declare const LAZY_PDF_PAGE_THRESHOLD = 12;
export type RenderedPdfPages = {
    pageWraps: HTMLElement[];
    canvases: HTMLCanvasElement[];
};
export type LazyPdfPagesHandle = {
    pageWraps: HTMLElement[];
    canvases: Map<number, HTMLCanvasElement>;
    rerenderAll: () => Promise<void>;
    destroy: () => void;
};
export declare function renderPdfPages(pdfDoc: PDFDocumentProxy, pagesRoot: HTMLElement, scale: number): Promise<RenderedPdfPages>;
/** Virtualized PDF pages — placeholders for all pages; canvas renders when visible. */
export declare function renderPdfPagesLazy(pdfDoc: PDFDocumentProxy, pagesRoot: HTMLElement, scale: number): Promise<LazyPdfPagesHandle>;
export declare function computeFitWidthScale(pdfDoc: PDFDocumentProxy, containerWidth: number, padding?: number): Promise<number>;
/** Scale so the full first page fits inside the viewport (width and height). */
export declare function computeFitPageScale(pdfDoc: PDFDocumentProxy, containerWidth: number, containerHeight: number, padding?: number): Promise<number>;
export declare function computePrintPageScale(page: PDFPageProxy): number;
export declare function renderPdfPageToCanvas(page: PDFPageProxy, scale: number): Promise<HTMLCanvasElement>;
//# sourceMappingURL=pdf-page-renderer.d.ts.map