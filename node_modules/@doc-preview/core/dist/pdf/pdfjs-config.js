let pdfjsLib = null;
let workerReady = false;
export async function getPdfJs() {
    if (!pdfjsLib) {
        pdfjsLib = await import("pdfjs-dist/legacy/build/pdf.mjs");
    }
    if (!workerReady && pdfjsLib.GlobalWorkerOptions) {
        const version = typeof pdfjsLib.version === "string" && pdfjsLib.version.length > 0
            ? pdfjsLib.version
            : "4.10.38";
        pdfjsLib.GlobalWorkerOptions.workerSrc = `https://unpkg.com/pdfjs-dist@${version}/legacy/build/pdf.worker.min.mjs`;
        workerReady = true;
    }
    return pdfjsLib;
}
//# sourceMappingURL=pdfjs-config.js.map