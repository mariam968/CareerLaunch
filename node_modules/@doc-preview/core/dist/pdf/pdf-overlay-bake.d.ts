/** Shared helpers to bake PDF overlay annotations (highlights, draws, redactions) into print/download output. */
export type RelativeRect = {
    rx: number;
    ry: number;
    rw: number;
    rh: number;
};
export type RelativeDrawPath = {
    points: {
        x: number;
        y: number;
    }[];
};
export declare function collectPdfHighlightsByPage(pagesTrack: HTMLElement, previewRoot: HTMLElement): Map<number, RelativeRect[]>;
export declare function collectPdfRedactionsByPage(pagesTrack: HTMLElement, previewRoot: HTMLElement): Map<number, RelativeRect[]>;
export declare function collectPdfDrawPathsByPage(pagesTrack: HTMLElement, previewRoot: HTMLElement): Map<number, RelativeDrawPath[]>;
export declare function drawHighlightsOnCanvas(ctx: CanvasRenderingContext2D, rects: RelativeRect[], width: number, height: number): void;
export declare function drawRedactionsOnCanvas(ctx: CanvasRenderingContext2D, rects: RelativeRect[], width: number, height: number): void;
export declare function drawPathsOnCanvas(ctx: CanvasRenderingContext2D, paths: RelativeDrawPath[], width: number, height: number): void;
/** SVG overlay for print/PDF export using 0–1 page-relative path coordinates. */
export declare function createPrintDrawSvg(paths: RelativeDrawPath[]): SVGSVGElement | null;
//# sourceMappingURL=pdf-overlay-bake.d.ts.map