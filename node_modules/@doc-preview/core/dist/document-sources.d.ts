import type { PreviewDocument } from "./types";
export type Base64DocumentInput = {
    /** Raw base64 or full `data:mime/type;base64,...` URL */
    base64: string;
    fileName?: string;
    /** Required when `base64` is raw (not a data URL) */
    mimeType?: string;
    fileType?: string;
};
export declare function isDataUrl(value: string): boolean;
/** Copy PDF/document bytes so pdf.js / pdf-lib cannot detach the only buffer. */
export declare function cloneArrayBuffer(source: ArrayBuffer): ArrayBuffer;
export declare function parseDataUrl(dataUrl: string): {
    mimeType: string;
    base64: string;
};
/** Decode base64 (or data URL) into a Blob. */
export declare function decodeBase64ToBlob(base64: string, mimeType?: string): Blob;
/** Build preview documents from one or more uploaded files. */
export declare function createPreviewDocumentsFromFiles(files: File[] | FileList): PreviewDocument[];
/** Build a preview document from a Blob (or File). */
export declare function createPreviewDocumentFromBlob(blob: Blob, options?: {
    fileName?: string;
    fileType?: string;
}): PreviewDocument;
/** Build a preview document from base64 or a data URL string. */
export declare function createPreviewDocumentFromBase64(input: Base64DocumentInput): PreviewDocument;
/** Normalize uri / base64 / file fields into a document with `file` set when possible. */
export declare function materializePreviewDocument(doc: PreviewDocument): PreviewDocument;
//# sourceMappingURL=document-sources.d.ts.map