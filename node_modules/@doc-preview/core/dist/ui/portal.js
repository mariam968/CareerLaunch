/** Mount overlays on document.body so they are not clipped by overflow:hidden ancestors. */
export function appendToPortal(node) {
    node.dataset.dpPortal = "true";
    globalThis.document.body.appendChild(node);
}
export function positionFixedBelow(anchor, el, gap = 6) {
    const rect = anchor.getBoundingClientRect();
    el.style.position = "fixed";
    el.style.top = `${rect.bottom + gap}px`;
    el.style.left = `${Math.max(8, rect.right - 220)}px`;
    el.style.zIndex = "2147483000";
}
export function createFixedBackdrop(className) {
    const backdrop = globalThis.document.createElement("div");
    backdrop.className = className;
    backdrop.style.position = "fixed";
    backdrop.style.inset = "0";
    backdrop.style.zIndex = "2147482999";
    return backdrop;
}
//# sourceMappingURL=portal.js.map