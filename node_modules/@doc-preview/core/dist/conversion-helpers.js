import { tryServerConversion } from "./conversion.js";
import { extractPdfDiffGeometry } from "./pdf/pdf-diff-geometry.js";
import { resolvePreviewDocument } from "./loaders/resolve-source.js";
import { detectDocumentType } from "./type-sniff.js";
import { readDocumentBuffer } from "./extract-comparable-text.js";
const LEGACY_OFFICE_FOR_CONVERSION = new Set([
    "doc",
    "application/msword",
    "ppt",
    "application/vnd.ms-powerpoint"
]);
const NATIVE_DIFF_TYPES = new Set([
    "pdf",
    "application/pdf",
    "docx",
    "docm",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document.macroEnabled.12",
    "application/vnd.ms-word.document.macroEnabled.12",
    "pptx",
    "pptm",
    "potx",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation",
    "application/vnd.openxmlformats-officedocument.presentationml.presentation.macroEnabled.12",
    "odt",
    "application/vnd.oasis.opendocument.text",
    "odp",
    "application/vnd.oasis.opendocument.presentation",
    "xlsx",
    "xlsm",
    "xltx",
    "xls",
    "ods",
    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    "application/vnd.ms-excel",
    "application/vnd.oasis.opendocument.spreadsheet"
]);
export async function isPdfTextLayerEmpty(doc, requestHeaders) {
    const type = detectDocumentType(doc);
    if (type !== "pdf" && type !== "application/pdf") {
        return false;
    }
    try {
        const buffer = await readDocumentBuffer(doc, requestHeaders);
        const geometry = await extractPdfDiffGeometry(buffer);
        return !geometry.text.trim();
    }
    catch {
        return true;
    }
}
/** Convert legacy Office or scanned PDF via config.conversion when native diff/search needs a text layer. */
export async function convertDocumentForNativePreview(doc, conversion, requestHeaders) {
    if (!conversion || conversion.convertForNativePreview === false) {
        return null;
    }
    const type = detectDocumentType(doc);
    const needsConversion = LEGACY_OFFICE_FOR_CONVERSION.has(type) ||
        ((type === "pdf" || type === "application/pdf") &&
            (await isPdfTextLayerEmpty(doc, requestHeaders)));
    if (!needsConversion) {
        return null;
    }
    const resolved = await resolvePreviewDocument(doc);
    const converted = await tryServerConversion(resolved, doc, conversion, requestHeaders);
    if (!converted) {
        return null;
    }
    return {
        ...doc,
        file: converted.blob,
        fileName: converted.fileName,
        fileType: converted.fileType
    };
}
export async function prepareSplitDiffDocuments(pair, conversion, requestHeaders) {
    const [left, right] = await Promise.all([
        convertDocumentForNativePreview(pair[0], conversion, requestHeaders),
        convertDocumentForNativePreview(pair[1], conversion, requestHeaders)
    ]);
    return [left ?? pair[0], right ?? pair[1]];
}
export function canUseNativeSplitDiffAfterConversion(left, right) {
    const a = detectDocumentType(left);
    const b = detectDocumentType(right);
    return a === b && NATIVE_DIFF_TYPES.has(a);
}
//# sourceMappingURL=conversion-helpers.js.map