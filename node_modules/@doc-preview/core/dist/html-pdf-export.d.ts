export type HtmlPdfExportOptions = {
    serverUrl: string;
    html: string;
    signal?: AbortSignal;
};
export type HtmlPdfExportResult = {
    blob: Blob;
    fileName: string;
};
/** Lightweight probe — OPTIONS is handled by the demo PDF server. */
export declare function probePdfExportServer(serverUrl: string, signal?: AbortSignal): Promise<boolean>;
/** POST HTML to a server-side renderer (demo: Playwright Chromium) and return PDF bytes. */
export declare function exportHtmlToPdf(options: HtmlPdfExportOptions): Promise<HtmlPdfExportResult>;
export declare function downloadBlob(blob: Blob, fileName: string): void;
//# sourceMappingURL=html-pdf-export.d.ts.map