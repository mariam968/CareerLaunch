import { getSegregationFeatureSections, segregationFeatureMatrix } from "./feature-segregation";
export { segregationFeatureMatrix, getSegregationFeatureSections };
/** Community (MIT) — full document preview across supported formats. */
export const communityFeatures = [
    "file-preview",
    "multi-document",
    "renderer-registry",
    "themes",
    "loading-phases",
    "request-headers",
    "blob-and-file-upload"
];
/** Enterprise — preview plus collaboration and security extras. */
export const enterpriseFeatures = [
    "annotations",
    "watermark",
    "split-view",
    "password-pdf",
    "print-with-watermark",
    "search-in-document",
    "redaction",
    "document-diff"
];
export function isCommunityFeature(feature) {
    return communityFeatures.includes(feature);
}
export function isEnterpriseFeature(feature) {
    return enterpriseFeatures.includes(feature);
}
//# sourceMappingURL=features.js.map