/** MIME types used by built-in preview renderers (for base64 / data-URL demos and tooling). */
export type PreviewMimeTypeOption = {
    value: string;
    /** Short label shown in dropdowns */
    label: string;
};
export type PreviewMimeTypeGroup = {
    label: string;
    options: readonly PreviewMimeTypeOption[];
};
export declare const PREVIEW_MIME_TYPE_GROUPS: readonly PreviewMimeTypeGroup[];
/** Flat list of all preview MIME type values (deduplicated). */
export declare const PREVIEW_MIME_TYPES: readonly string[];
export declare function isKnownPreviewMimeType(mime: string): boolean;
//# sourceMappingURL=preview-mime-types.d.ts.map