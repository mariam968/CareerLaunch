import { registerPreviewRenderer } from "./registry";
import { csvRenderer } from "./renderers/csv";
import { docxRenderer } from "./renderers/docx";
import { fallbackRenderer } from "./renderers/fallback";
import { htmlRenderer } from "./renderers/html";
import { imageRenderer } from "./renderers/image";
import { markdownRenderer } from "./renderers/markdown";
import { mediaRenderer } from "./renderers/media";
import { pdfRenderer } from "./renderers/pdf";
import { textRenderer } from "./renderers/text";
let registered = false;
export function registerBuiltinPreviewRenderers() {
    if (registered) {
        return;
    }
    registerPreviewRenderer(pdfRenderer);
    registerPreviewRenderer(docxRenderer);
    registerPreviewRenderer(imageRenderer);
    registerPreviewRenderer(mediaRenderer);
    registerPreviewRenderer(htmlRenderer);
    registerPreviewRenderer(csvRenderer);
    registerPreviewRenderer(markdownRenderer);
    registerPreviewRenderer(textRenderer);
    registerPreviewRenderer(fallbackRenderer);
    registered = true;
}
export const builtinPreviewRenderers = [
    pdfRenderer,
    docxRenderer,
    imageRenderer,
    mediaRenderer,
    htmlRenderer,
    csvRenderer,
    markdownRenderer,
    textRenderer,
    fallbackRenderer
];
//# sourceMappingURL=builtin-renderers.js.map