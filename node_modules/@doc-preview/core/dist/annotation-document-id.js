/** Stable per-document key for annotation/signature persistence. */
export function resolveAnnotationDocumentId(document, fallback) {
    const name = document?.fileName ?? document?.displayName;
    if (name && document?.file instanceof File) {
        return `${name}#${document.file.size}-${document.file.lastModified}`;
    }
    if (name) {
        return name;
    }
    return fallback ?? "document";
}
//# sourceMappingURL=annotation-document-id.js.map