import { type PreviewRenderer } from "@doc-preview/core";
import { type PptxPresentation, type PptxSlideContent } from "../pptx-slides.js";
export type PresentationDeckOptions = {
    formatLabel: string;
    partialBannerTitle?: string;
    /** Show yellow partial preview banner (default true). */
    showPartialPreviewBanner?: boolean;
    /** Show slide thumbnail strip toggle in toolbar (default true). */
    showThumbnails?: boolean;
    /** Enterprise: in-deck text search (current slide). */
    enableSearch?: boolean;
};
type PptxDeckSource = PptxSlideContent[] | PptxPresentation;
export declare const pptxDeckRenderer: PreviewRenderer;
export declare function renderPresentationDeck(wrap: HTMLElement, slides: PptxDeckSource, revokeUrls: string[], options: PresentationDeckOptions): () => void;
/** Build an off-screen slide canvas (for ZIP / batch PNG export). */
export declare function buildPptxSlideCanvas(slide: PptxSlideContent, revokeUrls: string[]): HTMLElement;
export {};
//# sourceMappingURL=pptx-deck.d.ts.map