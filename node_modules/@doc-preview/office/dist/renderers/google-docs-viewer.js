import { CLOUD_VIEWER_TYPES, renderCloudViewerFrame, supportsCloudViewer } from "../cloud-embed.js";
import { isPublicHttpUrl } from "../utils.js";
/** Google Docs viewer for public URLs (also available via office-online with cloudViewer: "google"). */
export const googleDocsViewerRenderer = {
    id: "google-docs-viewer",
    fileTypes: CLOUD_VIEWER_TYPES,
    weight: 7,
    strategy: "iframe",
    supports: (doc) => isPublicHttpUrl(doc.resolvedUri) && supportsCloudViewer(doc, "google"),
    render(container, { document: previewDoc, config, onError }) {
        renderCloudViewerFrame(container, previewDoc, {
            ...config,
            office: { ...config?.office, cloudViewer: "google" }
        }, onError);
    }
};
//# sourceMappingURL=google-docs-viewer.js.map