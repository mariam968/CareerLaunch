import { CLOUD_VIEWER_TYPES, renderCloudViewerFrame, supportsCloudViewer } from "../cloud-embed.js";
import { isPublicHttpUrl } from "../utils.js";
export const officeOnlineRenderer = {
    id: "office-online",
    fileTypes: CLOUD_VIEWER_TYPES,
    weight: 20,
    strategy: "iframe",
    supports(doc) {
        return (isPublicHttpUrl(doc.resolvedUri) &&
            (supportsCloudViewer(doc, "microsoft") || supportsCloudViewer(doc, "google")));
    },
    render(container, { document: previewDoc, config, onError }) {
        renderCloudViewerFrame(container, previewDoc, config, onError);
    }
};
//# sourceMappingURL=office-online.js.map