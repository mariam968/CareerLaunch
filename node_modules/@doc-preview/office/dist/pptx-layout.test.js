import { describe, expect, it } from "vitest";
import { countSlideAnimations, countSlideBuildSteps, getOccupiedPlaceholderKeys, listSlidePathsByFilename, parseMasterTxStyles, placeholderKey } from "./pptx-layout.js";
describe("countSlideAnimations", () => {
    it("counts timing and transition nodes", () => {
        const xml = `<?xml version="1.0"?>
<p:sld xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main">
  <p:timing><p:tnLst/></p:timing>
  <p:transition spd="med"/>
</p:sld>`;
        expect(countSlideAnimations(xml)).toBeGreaterThan(0);
    });
    it("returns zero for static slides", () => {
        const xml = `<?xml version="1.0"?><p:sld xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main"><p:cSld/></p:sld>`;
        expect(countSlideAnimations(xml)).toBe(0);
    });
});
describe("countSlideBuildSteps", () => {
    it("counts par nodes under timing", () => {
        const xml = `<?xml version="1.0"?>
<p:sld xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main">
  <p:timing><p:tnLst><p:par/><p:par/></p:tnLst></p:timing>
</p:sld>`;
        expect(countSlideBuildSteps(xml)).toBe(2);
    });
    it("returns zero when timing is absent", () => {
        const xml = `<?xml version="1.0"?><p:sld xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main"><p:cSld/></p:sld>`;
        expect(countSlideBuildSteps(xml)).toBe(0);
    });
});
describe("getOccupiedPlaceholderKeys", () => {
    it("marks placeholders that contain text", () => {
        const xml = `<?xml version="1.0"?>
<p:sld xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main"
       xmlns:a="http://schemas.openxmlformats.org/drawingml/2006/main">
  <p:cSld><p:spTree>
    <p:sp>
      <p:nvSpPr><p:nvPr><p:ph type="title"/></p:nvPr></p:nvSpPr>
      <p:txBody><a:p><a:r><a:t>Hello</a:t></a:r></a:p></p:txBody>
    </p:sp>
  </p:spTree></p:cSld>
</p:sld>`;
        const keys = getOccupiedPlaceholderKeys(xml);
        expect(keys.has("type:title")).toBe(true);
    });
});
describe("placeholderKey", () => {
    it("prefers type over idx", () => {
        const doc = new DOMParser().parseFromString('<p:ph xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main" type="body" idx="1"/>', "application/xml");
        expect(placeholderKey(doc.documentElement)).toBe("type:body");
    });
});
describe("parseMasterTxStyles", () => {
    it("reads title and body level styles from txStyles", () => {
        const xml = `<?xml version="1.0"?>
<p:sldMaster xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main"
             xmlns:a="http://schemas.openxmlformats.org/drawingml/2006/main">
  <p:txStyles>
    <p:titleStyle><a:defPPr><a:defRPr sz="4400" b="1"/></a:defPPr></p:titleStyle>
    <p:bodyStyle>
      <a:defPPr><a:defRPr sz="2800"/></a:defPPr>
      <a:lvl1pPr><a:defRPr sz="2400"/></a:lvl1pPr>
    </p:bodyStyle>
  </p:txStyles>
</p:sldMaster>`;
        const styles = parseMasterTxStyles(xml);
        expect(styles.titleDefPPr).toBeTruthy();
        expect(styles.bodyLevels.get(0)).toBeTruthy();
        expect(styles.bodyLevels.get(1)).toBeTruthy();
    });
});
describe("listSlidePathsByFilename", () => {
    it("sorts slide paths numerically", () => {
        const zipFiles = {
            "ppt/slides/slide10.xml": {},
            "ppt/slides/slide2.xml": {},
            "ppt/slides/slide1.xml": {}
        };
        const paths = listSlidePathsByFilename({ files: zipFiles });
        expect(paths).toEqual(["ppt/slides/slide1.xml", "ppt/slides/slide2.xml", "ppt/slides/slide10.xml"]);
    });
});
//# sourceMappingURL=pptx-layout.test.js.map