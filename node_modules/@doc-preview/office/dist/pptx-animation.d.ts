export type PptxSlideTransition = {
    effect: "fade" | "push" | "wipe" | "cut" | "none";
    durationMs: number;
};
export type PptxAnimationBuildStep = {
    spIds: string[];
    /** OOXML presetClass (entr, emph, exit, …). */
    effect?: string;
    /** OOXML presetSubtype (fromBottom, fromTop, fromLeft, fromRight, in, …). */
    presetSubtype?: string;
};
/** Slide transition metadata from p:transition (visual playback in deck preview). */
export declare function parseSlideTransition(slideXml: string): PptxSlideTransition;
/** Build sequence from slide timing — each step lists shape ids revealed on that click. */
export declare function parseSlideBuildSequence(slideXml: string): PptxAnimationBuildStep[];
/** Maps OOXML presetClass + presetSubtype to a CSS animation class suffix. */
export declare function buildAnimEffectClass(effect?: string, subtype?: string): string;
//# sourceMappingURL=pptx-animation.d.ts.map