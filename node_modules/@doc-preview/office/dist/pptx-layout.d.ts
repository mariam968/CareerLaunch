import type JSZip from "jszip";
export type MasterTxStyles = {
    titleDefPPr?: Element | null;
    bodyLevels: Map<number, Element>;
};
export type PartRelationship = {
    path: string;
    isImage: boolean;
    isSlideLayout: boolean;
    isSlideMaster: boolean;
    isSlide: boolean;
};
export declare function normalizePptPartPath(partPath: string, target: string): string;
export declare function loadPartRelationships(zip: JSZip, partPath: string): Promise<Map<string, PartRelationship>>;
export declare function findRelationshipByKind(rels: Map<string, PartRelationship>, kind: "layout" | "master" | "slide"): PartRelationship | undefined;
export declare function listSlidePathsInPresentationOrder(zip: JSZip): Promise<string[]>;
export declare function listSlidePathsByFilename(zip: JSZip): string[];
/** Count build/animation paragraphs inside slide timing (step-through preview). */
export declare function countSlideBuildSteps(slideXml: string): number;
/** Count animation/timing nodes (transitions and builds are not played). */
export declare function countSlideAnimations(slideXml: string): number;
/** Placeholder keys occupied by slide content (layout shapes for these are skipped). */
export declare function getOccupiedPlaceholderKeys(slideXml: string): Set<string>;
export declare function placeholderKey(ph: Element): string;
export declare function shapeHasPlaceholder(shape: Element): boolean;
export declare function shapePlaceholderKey(shape: Element): string | null;
export declare function resolveSlidePartChain(zip: JSZip, slidePath: string): Promise<{
    layoutPath?: string;
    masterPath?: string;
}>;
/** Default title/body paragraph styles from slide master txStyles. */
export declare function parseMasterTxStyles(masterXml: string): MasterTxStyles;
//# sourceMappingURL=pptx-layout.d.ts.map