/** ODT compare text aligned with rendered HTML block structure. */
export declare function extractOdtComparableText(buffer: ArrayBuffer): Promise<string>;
export type OdtComparableBlock = {
    text: string;
    tag: string;
    layoutKey: string;
};
/** Block + layout metadata for ODT native compare. */
export declare function extractOdtComparableBlocks(buffer: ArrayBuffer): Promise<OdtComparableBlock[]>;
/** Plain text from OpenDocument (.odt / .odp) — for document compare / diff. */
export declare function extractOpenDocumentPlainText(buffer: ArrayBuffer, detectedType: string): Promise<string>;
/** Plain text from legacy .ppt OLE — for document compare / diff. */
export declare function extractLegacyPptPlainText(buffer: ArrayBuffer): string;
/** Plain text from all sheets — for document compare / diff. */
export declare function extractSpreadsheetPlainText(buffer: ArrayBuffer): string;
/** Plain text from ODP deck — for document compare / diff. */
export declare function extractOdpPlainText(buffer: ArrayBuffer): Promise<string>;
/** Plain text from slide deck — for document compare / diff. */
export declare function extractPptxPlainText(buffer: ArrayBuffer): Promise<string>;
//# sourceMappingURL=extract-comparable.d.ts.map