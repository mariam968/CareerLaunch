import { describe, expect, it } from "vitest";
import { parseSlideBuildSequence, parseSlideTransition } from "./pptx-animation.js";
describe("parseSlideTransition", () => {
    it("reads fade transition speed", () => {
        const xml = `<?xml version="1.0"?>
<p:sld xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main">
  <p:transition spd="fast"><p:fade/></p:transition>
</p:sld>`;
        expect(parseSlideTransition(xml)).toEqual({ effect: "fade", durationMs: 400 });
    });
});
describe("parseSlideBuildSequence", () => {
    it("groups spTgt ids per timing par", () => {
        const xml = `<?xml version="1.0"?>
<p:sld xmlns:p="http://schemas.openxmlformats.org/presentationml/2006/main">
  <p:timing><p:tnLst><p:par><p:cTn nodeType="mainSeq"><p:childTnLst>
    <p:par><p:cTn presetClass="entr"><p:childTnLst><p:par><p:spTgt spid="4"/></p:par></p:childTnLst></p:cTn></p:par>
    <p:par><p:cTn presetClass="entr"><p:childTnLst><p:par><p:spTgt spid="5"/></p:par></p:childTnLst></p:cTn></p:par>
  </p:childTnLst></p:cTn></p:par></p:tnLst></p:timing>
</p:sld>`;
        expect(parseSlideBuildSequence(xml)).toEqual([
            { spIds: ["4"], effect: "entr" },
            { spIds: ["5"], effect: "entr" }
        ]);
    });
});
//# sourceMappingURL=pptx-animation.test.js.map