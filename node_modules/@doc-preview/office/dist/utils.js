export function isPublicHttpUrl(uri) {
    return /^https?:\/\//i.test(uri) && !uri.startsWith("blob:");
}
export function matchesType(doc, types) {
    return types.includes(doc.detectedType);
}
//# sourceMappingURL=utils.js.map