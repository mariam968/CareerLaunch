/** Rasterize a DOM subtree to PNG (slide export). Uses SVG foreignObject — no extra dependency. */
/** Render element to a PNG blob (for pre-cache or async save). */
export declare function exportElementToPngBlob(element: HTMLElement, scale?: number): Promise<Blob>;
/** Trigger a file download synchronously (must run in the same turn as a user click when possible). */
export declare function downloadPngBlob(blob: Blob, fileName: string): void;
/** Save PNG with File System Access API when available (works after async work). */
export declare function savePngBlob(blob: Blob, fileName: string): Promise<void>;
export declare function exportElementAsPng(element: HTMLElement, fileName: string, scale?: number): Promise<void>;
/** Download any blob (e.g. ZIP archive). */
export declare function downloadBlob(blob: Blob, fileName: string): void;
//# sourceMappingURL=export-slide-png.d.ts.map